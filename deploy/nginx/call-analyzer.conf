# Nginx конфигурация для Call Analyzer
# Разместите этот файл в /etc/nginx/sites-available/call-analyzer
# и создайте симлинк: ln -s /etc/nginx/sites-available/call-analyzer /etc/nginx/sites-enabled/

upstream call_analyzer_web {
    server 127.0.0.1:5000;
    keepalive 32;
}

server {
    listen 80;
    server_name your-domain.com;  # Замените на ваш домен или IP

    # Логи
    access_log /var/log/nginx/call-analyzer-access.log;
    error_log /var/log/nginx/call-analyzer-error.log;

    # Максимальный размер загружаемых файлов
    client_max_body_size 100M;

    # Проксирование на Flask приложение
    location / {
        proxy_pass http://call_analyzer_web;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        # Таймауты для долгих запросов (генерация отчетов)
        proxy_connect_timeout 300s;
        proxy_send_timeout 300s;
        proxy_read_timeout 300s;
    }

    # Статические файлы (если есть)
    location /static {
        alias /opt/call-analyzer/web_interface/static;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}

# Для HTTPS (раскомментируйте после настройки SSL)
# server {
#     listen 443 ssl http2;
#     server_name your-domain.com;
# 
#     ssl_certificate /path/to/cert.pem;
#     ssl_certificate_key /path/to/key.pem;
# 
#     # Остальная конфигурация как выше
#     ...
# }

